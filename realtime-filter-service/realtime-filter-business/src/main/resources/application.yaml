# HTTP Server
server:
 port: 3333   # HTTP port

# Spring properties
spring:
 application:
  name: realtime-filter-service

logging:
 file: logs/${spring.application.name}.log
 level:
   org.springframework.cloud: 'DEBUG'
   com.eduibanez.web: 'DEBUG'